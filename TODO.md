
## Core Event Bus

- [x] Implement the passive global Event Bus in `Core/` (`MoonGame/Assets/Scripts/Core/EventBus/`) so inter-domain communication is possible without cross-domain references. Static `EventBus` facade over `internal EventBusCore`; `readonly struct : IEvent` events; sync main-thread `Publish(in T)`; `EventBusSubscriber` MonoBehaviour for auto-cleanup. Note: `Core.asmdef.meta` GUID is generated by Unity on first import and is the `core_guid` required by `scaffold_domain`.

## Verification

- [ ] Grep all domain scripts for cross-domain `using` statements; confirm zero violations.
- [ ] Confirm every domain folder has a `.asmdef` referencing only Core via GUID.
- [ ] Confirm every conditional route in `.opencode/config.json` resolves to an existing doc file.
