
## Player Controller — Next Milestone

- [ ] Run the Phase 1 whitebox traversal matrix in `MoonGame/Assets/Scenes/SampleScene.unity` and record the result in `docs/roadmaps/player-controller.md`.

## Repository Verification

- [ ] Confirm every conditional route in `.opencode/config.json` resolves to an existing doc file.
- [ ] When a gameplay domain is created, confirm its `.asmdef` references only Core by GUID.
- [ ] When gameplay domains contain scripts, grep them for cross-domain `using` statements and confirm zero violations.
